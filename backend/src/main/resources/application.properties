spring.application.name=backend

spring.datasource.url=jdbc:postgresql://localhost:5432/bajrix
spring.datasource.username=bajrix
spring.datasource.password=bajrix123

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

spring.flyway.enabled=true

server.port=8080

# Admin configuration
bajrix.admin.username=${ADMIN_USERNAME:admin}
bajrix.admin.password=${ADMIN_PASSWORD:BajriX@Admin123}